# OpenAI
OPENAI_API_KEY=sk-...

# Model IDs (override if needed)
MODEL_REASONING=gpt-4o
MODEL_FAST=gpt-4o-mini

# Wassist (WhatsApp transport) — needed only for real WhatsApp
WASSIST_API_KEY=
WASSIST_BASE_URL=https://alpha-plagiocephalic-darrell.ngrok-free.dev
WASSIST_WEBHOOK_SECRET=
# Public URL where Wassist will POST inbound messages (e.g. Railway HTTPS URL + /webhook)
PUBLIC_WEBHOOK_URL=

# Local server
PORT=8787

# Postgres (Railway public URL or internal DATABASE_URL)
DATABASE_URL=postgresql://user:password@host:5432/railway
